<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ css_href }}">
  <meta name="generator" content="AI Business Dashboard" />
</head>
<body>
  <header class="report-header">
    <div class="brand">
      <h1>{{ title }}</h1>
      <p class="subtitle">{{ subtitle }}</p>
    </div>
    <div class="meta">
      <div><span>Report ID:</span> {{ report.id }}</div>
      <div><span>Type:</span> {{ report.type }}</div>
      <div><span>Status:</span> {{ report.status }}</div>
      <div><span>Generated On:</span> {{ report.generated_on }}</div>
      <div><span>Generated By:</span> {{ report.generated_by or "—" }}</div>
    </div>
  </header>

  <section class="kpis">
    <div class="kpi">
      <div class="kpi-label">Total Employees</div>
      <div class="kpi-value">{{ stats.total_employees }}</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">Avg Efficiency</div>
      <div class="kpi-value">{{ stats.avg_efficiency }}%</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">Total Leads</div>
      <div class="kpi-value">{{ stats.total_leads }}</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">Conversion Rate</div>
      <div class="kpi-value">{{ stats.conversion_rate }}%</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">Total Projects</div>
      <div class="kpi-value">{{ stats.total_projects }}</div>
    </div>
  </section>

  <section class="summary">
    <h2>Executive Summary</h2>
    <p>{{ summary }}</p>
  </section>

  <section class="details">
    <h3>Highlights</h3>
    <ul>
      <li>Employees: {{ stats.total_employees }} with average efficiency {{ stats.avg_efficiency }}%.</li>
      <li>Leads: {{ stats.total_leads }} total; conversion {{ stats.conversion_rate }}%.</li>
      <li>Projects: {{ stats.total_projects }} tracked; status mix reflects current delivery cadence.</li>
    </ul>
  </section>

  <footer class="report-footer">
    <div>Source: AI Business Dashboard • Generated {{ generated_at }}</div>
  </footer>
</body>
</html>
